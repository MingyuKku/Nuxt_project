export default function({store, route, redirect}) {
  // console.log('네임미들웨어', store.state.isLogin)
  // console.log('미들웨어 라우터', route)
  // if(!store.state.isLogin) {
  //   return redirect('/');
  // }
}

